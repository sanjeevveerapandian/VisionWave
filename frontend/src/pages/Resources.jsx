import React from 'react';
import ResourceList from '../components/ResourceList';

const Resources = () => {
    return (
        <div>
            <ResourceList />
        </div>
    );
};

export default Resources;
